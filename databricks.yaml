bundle:
  name: hackathon2026-app

sync:
  include:
    - frontend/dist/**
  exclude:
    - bootstrap/**

resources:
  apps:
    hackathon2026-app:
      name: databeat
      description: "Databeat."
      source_code_path: .


targets:
  dev:
    default: true
    mode: development

  prod:
    mode: production
